[dns]
port = 15353
protocol = "udp"

    [dns.soa]
    ttl = 300
    refresh = 3600
    retry = 1800
    expire = 10800
    minimum = 300
    mname = "dns.ellefsen.ninja"
    rname = "dns.ellefsen.ninja"

    [dns.ns]
    servers = ["ns-ipv6.ellefsen.ninja"]

[domains]
    [domains."ipv6.ellefsen.ninja"]
    prefix = "2001:470:1f23:ff::"
    mask = 64

